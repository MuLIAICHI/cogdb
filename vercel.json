{
  "buildCommand": "uv pip install --system -r requirements-docs.txt && mkdocs build --site-dir public/docs && cp -r website/. public/",
  "outputDirectory": "public",
  "installCommand": "uv pip install --system -r requirements-docs.txt"
}
