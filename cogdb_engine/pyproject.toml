[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "cogdb_engine"
requires-python = ">=3.10"
version = "0.1.0"

[tool.maturin]
features = ["python"]
module-name = "cogdb_engine"
